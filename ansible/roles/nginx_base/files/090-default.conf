# vim: ft=nginx ts=4 sts=4 sw=4 et:

server {
    listen 0.0.0.0:80;
    listen [::]:80;

    location / {
        return 301 https://$host$request_uri;
    }

    include "/etc/nginx/letsencrypt.conf";
}
